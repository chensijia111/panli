<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cn.mapper.UserMapper">

    <select id="queryUserById" parameterType="int" resultType="com.cn.pojo.User">
        select * from user where id = #{id}
    </select>

    <select id="queryAllUser" resultType="com.cn.pojo.User">
        select * from user
    </select>

    <insert id="insertUser" parameterType="com.cn.pojo.User">
        insert into user(username,password,gender) values (#{username},#{password},#{gender})
    </insert>

    <update id="updateUser" parameterType="com.cn.pojo.User">
        update user set username=#{username},password=#{password},gender=#{gender} where id=#{id}
    </update>

    <delete id="deleteUserById" parameterType="java.lang.Integer">
        delete from user where id=#{id}
    </delete>

</mapper>
