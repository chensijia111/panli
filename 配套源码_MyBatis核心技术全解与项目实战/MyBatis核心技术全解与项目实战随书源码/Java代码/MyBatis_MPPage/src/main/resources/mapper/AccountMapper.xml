<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.mapper.AccountMapper">

    <select id="selectPageByMoney" resultType="com.cn.pojo.Account">
        SELECT a_id AS id,a_name AS name,a_money AS money,a_version AS version
               FROM t_account WHERE a_money > #{money}
    </select>

</mapper>
